# Request Tracker 4.0

## Installation
Install guide here: [[http://requesttracker.wikia.com/wiki/DebianSqueezeInstallGuide]]

*tl;dr:* squeeze-backports required

1. Install everything.

        sudo aptitude install mysql-server rt4-db-mysql rt4-apache2 request-tracker4

* Configuration:
     - Name: **$RTSERVER**.ocf.berkeley.edu
     - Handle RT_SiteConfig.pam permissions: yes
     - SQL password: ....
7. Add the following lines to /etc/apache2/sites-available/default before the ending VirtualHost tag.

        Include /etc/request-tracker4/apache2-modperl2.conf

8. Restart Apache.

9. Supposing the server RT is installed on isn't also the mail server, install the RT [mailgate program](http://requesttracker.wikia.com/wiki/EmailInterface#RT.27s_mail_gate) on the mail server.

  squeeze-backports required here too.

        sudo aptitude install rt4-clients 

10. Set up rt@ to feed mail into rt-mailgate.

  This implies that the setup desired is that there is an actual OCF user named rt, who has a real home directory, mail account, *etc.* **Edit $RTSERVER below!**

        sudo -u rt sh -c \
        'echo "\"|/usr/bin/rt-mailgate --SOME-MISSING-ARGUMENTS? \
        --url https://$RTSERVER.ocf.berkeley.edu\"" > ~/.forward'


## Todo
* Actually install rt4-clients on sandstorm
* Find out how to make RT conform to our desired workflow
